# from prometheus_client import Histogram

# inference_duration_hist = Histogram(
#     'inference_duration_seconds',
#     'Time taken for model inference (seconds)',
#     buckets=[0.1, 0.3, 0.5, 1, 2, 3, 5, 10]
# )

#тут у меня не очень получилось в итоге